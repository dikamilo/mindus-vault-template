---
title: flag/stale
tags: [meta/tag-definition]
tag: flag/stale
kind: flag
layers: []
---

# flag/stale

You believe this note needs a fresh look, independent of the automatic `stale-note` check (which fires on age alone). Applied by hand only; see `flag/contested` for how `stale-flag` treats flags generally.
